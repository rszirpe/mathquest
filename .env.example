# Copy to .env and fill in to enable live Gemini calls in the mastery loop.
# Without this set, src/lib/ai/index.ts falls back to the stub client automatically.
# Get a key at https://aistudio.google.com/apikey
VITE_GEMINI_API_KEY=
